# Void-coding

This repository now contains a minimal Next.js App Router + TypeScript + Tailwind CSS scaffold.

Run locally:

```bash
git clone https://github.com/Ayushbuilds12/Void-coding.git
cd Void-coding
npm install
npm run dev
```

The app will run at http://localhost:3000

Ready for deployment on Vercel — just import the repository.
